# 登录、账号与会话标准

Design Standard: Leego Design UI
Standard ID: leego-design-ui
Version: 2.14.0

## 一个能力，一个主要入口

| 平台 | 账号身份 | 退出 | 登录页 |
| --- | --- | --- | --- |
| 企业 Web | 顶栏头像、姓名与角色；窄屏收为头像 | 顶栏最右侧，≤760px 收进抽屉 | 单列居中，不带工作台侧栏 |
| Mobile | “我的”页 | “我的”页底部独立一行 | 同一表单顺序，响应系统字号 |
| Pad | 专用导航的账号入口 | 账号面板内 | 沿用 Pad 触控标准 |
| 公开官网 | 仅在真实产品需要时提供 | 不强加后台账号外壳 | 不为浏览规范增加登录门槛 |

不要在桌面侧栏底部再次放同一个头像、姓名和退出。顶栏存在当前路径时，不再添加介绍性标题。一个全局搜索入口；页面筛选必须说明具体对象范围。全局操作与内容主操作各有自己的层级，不机械限制整个页面只能有一个按钮。

## 固定登录模板

- 表单最大宽 400px，容器内边距 24px（窄屏 20px）、字段间距 16px，输入与主操作最小高度 48px。不通过大面积插画或营销文案干扰登录。
- 顺序固定：产品身份 → 登录标题 → 必要状态 → 真实认证方式 → 字段 → 主操作 → 实际可用的找回/帮助入口。只显示项目确有的密码、验证码或企业 SSO，不制造接口。
- 字段有可见标签；错误紧邻字段或表单，不能只靠红边。禁止用 placeholder 代替标签，禁止禁用粘贴或密码管理器。真实密码表单使用 username/current-password 自动填充；演示账号为只读，不收集用户真实凭证。
- 忘记密码、注册、记住登录、显示密码不是强制装饰；有能力才出现。纯图标控件提供名称、焦点、≥44px 热区。
- 默认、焦点、提交中、校验错误、服务失败、成功跳转可观察。提交期间防止重复提交，失败可重试；不得直接展示服务堆栈或泄露账号是否存在。

## 会话状态与退出

`初始化 → 未登录 → 登录中 → 已登录 → 退出中 → 未登录`

- 初始化不闪现受保护业务内容。登录成功只返回经过校验的站内目标；拒绝外域、协议相对路径和非法 returnTo。
- 退出调用项目已有会话注销能力，成功后清理应用自身缓存与受保护内容，再进入登录。不能仅 toast“已退出”，也不能全局 localStorage.clear() 破坏其他应用。
- 网络失败不能谎称退出成功：显示“退出未完成”及重试，按真实会话状态处理。403 是权限不足，不等于会话过期；不得产生重定向死循环。
- 过期时隐藏受保护内容，解释“需要重新登录”，保留允许保存的站内返回路径和非敏感筛选。重新认证后再获取授权数据。密码、会话令牌、敏感表单不可放进持久化 UI 状态。
- 普通退出不反复确认；仅有真实未保存工作、上传或不可恢复风险时询问保存/放弃，不新增每次必弹的确认框。
- 中止请求或卸载组件时清理副作用；回退、刷新、跨标签页注销按照现有认证协议验证，不把前端布尔值当权限保障。

## 原型边界

规范站保持公开可访问；仅在示例画布内演示登录与退出。演示内容使用固定虚构身份，标记“原型演示”，不调用认证服务。为了展示恢复上下文，演示可以保留已隐藏的虚构任务组件；**生产系统不能照搬这种保留受保护 DOM 的实现**，必须按实际认证能力清理内容、缓存和请求。

无搜索、通知、账号或退出回调时，组件隐藏该操作或提供明确不可操作文本。不能呈现“可以点击但毫无反应”的假按钮。快捷键仅作用于当前可见工作台，无活动对话框时优先聚焦可见搜索，否则打开搜索面板。

## 减法检查与保护

对每个信息点指定唯一责任区：路径属于顶栏、范围属于筛选、状态属于状态条、证据属于详情、账号属于平台账号入口。重复呈现只有在滚动距离、可访问性或独立任务需要时才保留，并说明原因。

优先删除：重复身份、重复全局搜索、重复用途介绍、同一 KPI 的文字复述、无层级价值的卡片套卡片。保护：对象、范围、更新时间、风险、权限、证据和错误恢复。信息密度不能通过低于12px的文字、过浅对比或过小触控区实现。

## 验收

P0：假退出、敏感内容泄漏、登录循环、丢失重要任务、隐藏搜索抢焦点、能力不存在却可点击。P1：重复身份、表单次序漂移、图标热区/字号/间距不一致。

检查正常登录、校验错误、重复提交、失败重试、退出成功、退出失败、会话过期、403、返回路径、键盘焦点及200%缩放。只验证项目存在的认证方式，不为验收新增后台。

参考：会话注销与客户端缓存边界依据 [OWASP Session Management](https://cheatsheetseries.owasp.org/cheatsheets/Session_Management_Cheat_Sheet.html)；密码管理器、粘贴和无认知负担认证参考 [WCAG 2.2 Accessible Authentication](https://www.w3.org/WAI/WCAG22/Understanding/accessible-authentication-minimum.html)。本规范定义前端契约，不替代系统认证安全审计。
